- page_title 'Edit Application'
- breadcrumb link_to('Applications', applications_path), h(@applicant.character_info.current_name), 'Edit'
- semantic_form_for @applicant, :url => application_path(@applicant) do |form|
  = render :partial => 'form', :object => form