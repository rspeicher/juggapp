require 'spec_helper'

describe ApplicantSystem do
  before(:each) do
    @valid_attributes = {
      
    }
  end

  it "should create a new instance given valid attributes" do
    ApplicantSystem.create!(@valid_attributes)
  end
end
